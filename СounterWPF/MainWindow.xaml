<Window x:Class="СounterWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:СounterWPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button Name="decreaseButton" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="30"
                    Command="{Binding DecrementCommand}" Margin="144,0,0,0" RenderTransformOrigin="0.03,0.453">Уменьшить</Button>
            <Button Name="increaseButton" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="30"
                    Command="{Binding IncrementCommand}" RenderTransformOrigin="-0.211,0.538">Увеличить</Button>
        </Grid>
        <TextBlock Text="{Binding Count}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="40"></TextBlock>

    </Grid>
</Window>
